[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'uv run python -m maverick_mcp.api.server --transport streamable-http --host 0.0.0.0 --port ${PORT}'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
